#include <bits/stdc++.h>
using namespace std;

int main() {
    vector <int> v;
    v.push_back(2);
    v.emplace_back(3);
    vector<pair<int,char>>p;
    p.push_back({2,'a'});
    p.emplace_back(2,'a');
    vector<int> v(5,100);
    

    return 0;
}