    // vector<int> a;
    // a.push_back(1);
    // a.push_back(54);
    // a.push_back(15);
    // a.push_back(47);
    // a.push_back(65);
    // // sort(a,a+n);
    // sort(a.begin(),a.end());
    // for(auto it:a){
    //     cout<<it<<" ";
    // }
    // sort(a.begin(),a.end(),greater<int>());
    // for(auto it:a){
    //     cout<<it<<" ";
    // }
    // pair<int,int> a[] = {{5,6},{6,7},{5,7}};
    // //sort(a,a+n,comp)
    // //comp = self built comprator