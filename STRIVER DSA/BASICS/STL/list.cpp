#include <bits/stdc++.h>
using namespace std;

int main() {

    list <int>ls;
    ls.push_back(5);
    ls.emplace_back(7);
    ls.push_front(3);
    ls.emplace_front();

    deque <int> dq;

    // all func same as vector for both 
    
    return 0;
}